<section id="inicio" class="hero-home relative overflow-hidden border border-primary-800 bg-link">
  <img
    src="/img/OHI-entrada.webp"
    alt="Entrada de Organización Humana Integral en Valledupar"
    class="hero-home-image"
    loading="eager"
    fetchpriority="high"
    decoding="async"
  />
  <div class="pointer-events-none absolute inset-0 bg-linear-to-br from-primary-900/90 via-primary-700/76 to-primary-500/36"></div>
  <div class="pointer-events-none absolute inset-x-0 bottom-0 h-32 bg-linear-to-t from-primary-900/45 to-transparent"></div>

  <div class="mx-auto max-w-7xl px-5 py-16 lg:py-24">
    <article class="relative z-10 max-w-4xl space-y-6">
      <p class="inline-flex rounded-full border border-accent-400/50 bg-white/10 px-3 py-1 text-xs font-extrabold uppercase tracking-[0.18em] text-accent-300 shadow-sm backdrop-blur">
        Organización Humana Integral
      </p>
      <h1 class="max-w-5xl text-4xl font-black leading-tight text-white sm:text-5xl lg:text-7xl">
        No hay mejor cuidado para la salud como el que te hace sentir en casa.
      </h1>
      <p class="max-w-3xl text-base leading-8 text-white/85 md:text-lg">
        Atención integral, segura y oportuna para la región, con tecnología médica avanzada y un talento humano que acompaña cada etapa de cuidado con cercanía.
      </p>
      <div class="flex flex-col gap-3 sm:flex-row">
        <a href="#servicios" class="rounded bg-cta px-5 py-3 text-center text-sm font-bold text-primary-800 transition hover:bg-accent-400">
          Ver servicios
        </a>
        <a href="/quienes-somos" class="rounded border border-white/35 bg-white/10 px-5 py-3 text-center text-sm font-bold text-white transition hover:border-accent-300 hover:bg-white/15">
          Conoce OHI
        </a>
      </div>
    </article>

  </div>
</section>

<style>
  .hero-home {
    min-height: calc(100vh - 4.75rem);
    background: #052e87;
    display: flex;
    align-items: center;
  }

  .hero-home-image {
    position: absolute;
    inset: 0;
    height: 100%;
    width: 100%;
    object-fit: cover;
  }

  .hero-home h1,
  .hero-home p {
    text-shadow: 0 4px 20px rgba(0, 0, 0, 0.25);
  }
</style>